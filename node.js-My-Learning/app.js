// ? Build in modules in Node.js
// Are modules that come pre-installed with Node.js

// ? Event Loop
// Is what allows Node.JS to perform async (non-blocking) I/O (input/output) operations
// Event Loop in Node.JS works almost the same as ASYNC code in Vanilla JS

// ? Streams Module
// A stream lets you to process data piece by piece, instead of loading everyting into memory

// We use streams for: low memory usage, faster for big files, perfect for servers

// ? Streams ARE events under the hood.
